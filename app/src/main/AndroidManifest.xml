<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dbsrm.todolistdsc">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".secondwalkthrfragment" ></activity>
        <activity android:name=".firstwalkthrfragment" />
        <activity android:name=".EditToDo" />
        <activity android:name=".LoginActivity" />
            <activity android:name=".walkthrmain"/>
                <activity android:name=".splashscreen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TodoHomeActivity" />
        <activity android:name=".NewTask" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>